services:
  backend:
    build: ./home-inspection
    ports:
      - "8080:8080"
    environment:
      - PDF_SERVICE_URL=http://pdf-service:3001
    depends_on:
      - pdf-service

  pdf-service:
    build: ./pdf-service
    ports:
      - "3001:3001"